<!DOCTYPE html>
<html>
  <head>
    <title>StatusApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      .key { color: red; font-weight: bold; }
      .value { color: blue; font-weight: bold; }
      .numbers { color: purple; font-weight: bold; }
      .rails-key { color: darkred; font-weight: bold; }
      .rails-value { color: darkgreen; font-weight: bold; }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
